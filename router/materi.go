package router

import (
	"github.com/gin-gonic/gin"
	// "goadmin_starter_pack/middleware"
)

func RouterMateri(r *gin.Engine) {
	// rDashboard := r.Group("/dashboard")
	// rDashboard.Use(middleware.Auth)
	// rDashboard.GET("/", controller.Dashboard)

}

